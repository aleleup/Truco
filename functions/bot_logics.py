def bot_in_envido(pc_options: dict, game_instance) -> str:
    '''
    Function that will only execute if game instance == 1;
    Then there are 4 instanse in envido:

       '''
    
    if game_instance != 1: 
        return 
    