# Card = dict[str, str | int]
# Deck = list[Card]
Bet = str
PlayerStatus = dict[str, int | list[dict[str, int | str]]]
# PlayerAction = Card | Bet
# Movement = dict[str, bool | PlayerAction]
# Options = dict[int, Card | Bet]
